""" """


    
def simplify(v, row, row_n, i_s, i_d, header_s, header_d, scratch, errors, accumulator):
    """ Simplify geometry, so the grud squares will have only 5 points, 
    rather than 9 to 12.
    """

    # here is another
    return v.shape.simplify(25)
    